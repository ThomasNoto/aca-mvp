namespace backend.Models
{
    // user roles used in determining front UI
    // Admin role grants token to access create flight form
    public enum UserRole
    {
        Admin,
        User
    }
}
